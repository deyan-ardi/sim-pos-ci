'use strict';
$(document).ready(function() {

    // [ new-scroll ] start
    var px = new PerfectScrollbar('.new-scroll', {
        wheelSpeed: .5,
        swipeEasing: 0,
        wheelPropagation: 1,
        minScrollbarLength: 40,
    });
    // [ new-scroll ] end



});
